[project]
name = "ezsmolagents"
version = "0.1.0"
description = "EZ Smolagents makes smolagents easy."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "docker>=7.1.0",
    "rich>=15.0.0",
    "ruff>=0.15.12",
    "smolagents[litellm]>=1.24.0",
]
